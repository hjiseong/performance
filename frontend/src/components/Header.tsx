import "./Header.scss";
import React, { ReactElement } from "react";

interface Props {}

export default function Header({}: Props): ReactElement {
  return (
    <header>
      <div></div>
    </header>
  );
}
